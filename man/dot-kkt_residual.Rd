% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacking.R
\name{.kkt_residual}
\alias{.kkt_residual}
\title{KKT residual of the constrained problem}
\usage{
.kkt_residual(w, g, active_tol = 1e-10)
}
\arguments{
\item{w}{A numeric weight vector on the simplex.}

\item{g}{The gradient at \code{w}.}

\item{active_tol}{Weights above this are treated as active.}
}
\value{
A non-negative scalar, or NA when no weight is active.
}
\description{
For min f(w) subject to sum(w) = 1 and w >= 0 the conditions are: some lambda
with g_k = lambda wherever w_k > 0, and g_k >= lambda wherever w_k = 0. The
residual below is zero exactly at an optimum and is scale free, so it can
certify the answer where a stalled objective cannot.
}
\keyword{internal}
