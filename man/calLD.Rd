% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LD.R
\name{calLD}
\alias{calLD}
\title{Calculate LD matrix for BRIER.S}
\usage{
calLD(X, sumstats, LDB = NULL, tau = 0)
}
\arguments{
\item{X}{An n x p reference genotype matrix (individual-level).}

\item{sumstats}{A data.frame of marginal summary statistics. Required when
\code{LDB} is supplied; must contain \code{CHR}, \code{BP}, and
\code{corr} columns.}

\item{LDB}{Optional m x 3 matrix of LD block boundaries (\code{chr},
\code{start}, \code{stop}), e.g. from Berisa et al. If NULL, the full
p x p LD matrix is computed.}

\item{tau}{A numeric scalar. Soft-threshold parameter applied to LD
entries; entries with |LD| < tau are set to zero. Defaults to 0
(no thresholding).}
}
\value{
An object of class \code{"LD"} containing:
\describe{
\item{XtX}{The p x p sparse LD matrix.}
\item{blk}{The block boundary vector (NULL if no \code{LDB} supplied).}
\item{nz}{Integer vector of non-constant column indices retained.}
}
}
\description{
Compute a sparse LD reference matrix from a genotype reference panel, with
optional soft-thresholding and block-structured LD pruning. Used as input
to \code{\link{BRIERs}}.
}
\examples{
\dontrun{
# full LD matrix
ld <- calLD(X, tau = 0.05)

# block-structured LD with Berisa et al. blocks
ld <- calLD(X, sumstats = sumstats, LDB = LDB, tau = 0.05)
}

}
\seealso{
\code{\link{BRIERs}}, \code{\link{p2cor}}
}
