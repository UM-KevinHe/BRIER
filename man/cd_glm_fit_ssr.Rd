% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{cd_glm_fit_ssr}
\alias{cd_glm_fit_ssr}
\title{Coordinate descent for GLM with sequential strong rule}
\usage{
cd_glm_fit_ssr(
  X,
  y,
  family,
  multiplier,
  penalty,
  lambda,
  alpha,
  gamma,
  lam_max,
  max_iter,
  dfmax,
  eps,
  user = FALSE,
  warn = TRUE
)
}
\arguments{
\item{X}{A numeric matrix of predictors (n x p).}

\item{y}{A numeric response vector of length n.}

\item{family}{A string: "binomial" or "poisson".}

\item{multiplier}{A numeric vector of per-variable penalty multipliers (length p).
Variables with multiplier = 0 are treated as unpenalised.}

\item{penalty}{A string specifying the penalty: "LASSO", "MCP", or "SCAD".}

\item{lambda}{A numeric vector of penalty values, sorted in decreasing order.}

\item{alpha}{A numeric scalar for the elastic net mixing parameter.}

\item{gamma}{A numeric scalar for MCP and SCAD penalties.}

\item{lam_max}{A numeric scalar for the maximum lambda value (used in SSR cutoff).}

\item{max_iter}{An integer specifying the maximum total iterations allowed.}

\item{dfmax}{An integer specifying the maximum number of selected variables.}

\item{eps}{A numeric scalar for the convergence tolerance.}

\item{user}{A logical flag. If TRUE, lambda is user-specified and iteration starts
from the first value; if FALSE, the first lambda is treated as the null model.}

\item{warn}{A logical flag. If TRUE, issues a warning when the model saturates.}
}
\value{
A list with the following elements:
\describe{
\item{beta0}{A numeric vector of intercepts for each lambda.}
\item{beta}{A matrix of coefficients (p x length(lambda)).}
\item{dev}{A numeric vector of deviance values.}
\item{eeta}{A matrix of fitted linear predictors (n x length(lambda)).}
\item{iter}{An integer vector of iteration counts for each lambda.}
}
}
\description{
Coordinate-descent algorithm for penalised generalised linear models (binomial
or Poisson) with sequential strong rule (SSR) screening over a sequence of
penalty values.
}
