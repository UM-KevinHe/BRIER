% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BRIERi.R
\name{BRIERi_fit}
\alias{BRIERi_fit}
\title{Core fitting function for BRIER.I}
\usage{
BRIERi_fit(
  X,
  y,
  weights = NULL,
  eta = 0,
  y.external = NULL,
  family = c("gaussian", "binomial", "poisson"),
  penalty.factor = rep(1, ncol(X)),
  penalty = c("LASSO", "SCAD", "MCP"),
  alpha = 1,
  gamma = ifelse(penalty == "SCAD", 3.7, 3),
  n.sis = NULL,
  lambda,
  nlambda = 100,
  lambda.min = {
     if (nrow(X) > ncol(X)) 
         1e-04
     else 0.05
 },
  log.lambda = TRUE,
  max.iter = 1e+06,
  eps = 1e-04,
  nvar.max = NULL,
  returnX = FALSE
)
}
\arguments{
\item{X}{A numeric matrix of predictors (n x p).}

\item{y}{A numeric response vector of length n.}

\item{weights}{Optional numeric vector of observation weights (length n).
Defaults to equal weights.}

\item{eta}{A numeric vector of external-model weights (length M).
Use \code{eta = 0} to ignore external information.}

\item{y.external}{Optional numeric matrix of external predictions (n x M).}

\item{family}{A string: "gaussian", "binomial", or "poisson".}

\item{penalty.factor}{A numeric vector of per-variable penalty multipliers
(length p). Variables with multiplier 0 are unpenalised.}

\item{penalty}{A string specifying the penalty: "LASSO", "MCP", or "SCAD".}

\item{alpha}{A numeric scalar for the elastic net mixing parameter.}

\item{gamma}{A numeric scalar for MCP and SCAD penalties.}

\item{n.sis}{Optional. Reserved for future SIS extensions.}

\item{lambda}{Optional user-specified lambda sequence.}

\item{nlambda}{An integer specifying the number of lambda values.}

\item{lambda.min}{A numeric scalar for the minimum lambda ratio.}

\item{log.lambda}{Logical. If TRUE, the lambda sequence is log-spaced.}

\item{max.iter}{An integer specifying the maximum total iterations.}

\item{eps}{A numeric scalar for the convergence tolerance.}

\item{nvar.max}{An integer specifying the maximum number of selected variables.}

\item{returnX}{Logical. If TRUE, the standardised data list is returned in
the output for diagnostics.}
}
\value{
An object of class \code{"BRIER.fit"} containing model specification,
coefficients, fit statistics, and data.
}
\description{
Internal workhorse that fits a penalized regression model for a single eta
vector. Called by \code{\link{BRIERi}}, \code{\link{BRIERfull}}, and
\code{\link{BRIERi.cv}}. Performs response/predictor validation,
standardisation, lambda path setup, and dispatches to the appropriate
coordinate-descent C++ routine.
}
\seealso{
\code{\link{BRIERi}}, \code{\link{BRIERfull}},
\code{\link{coef.BRIER.fit}}, \code{\link{predict.BRIER.fit}}
}
\keyword{internal}
