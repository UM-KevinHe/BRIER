% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bopt.R
\name{BRIERs.bopt}
\alias{BRIERs.bopt}
\title{Bayesian optimization of eta for BRIER.S}
\usage{
BRIERs.bopt(
  sumstats,
  XtX,
  family = c("gaussian", "binomial", "poisson"),
  beta.external = rep(0, nrow(sumstats)),
  multi.method = c("ind", "PCA", "stacking"),
  optim.args = list(),
  criteria = c("Cp", "GIC", "pseu.val", "gaussian.mspe", "gaussian.rsq", "binomial.dev",
    "binomial.mcfrsq", "binomial.tjursq", "binomial.auc", "poisson.dev"),
  X.val = NULL,
  y.val = NULL,
  XtX.val = NULL,
  sumstats.val = NULL,
  TN = NULL,
  h2 = NULL,
  bounds = NULL,
  init.grid = NULL,
  init.points = 10L,
  n.iter = 15L,
  acq = "ucb",
  kappa = 2.576,
  acq.eps = 0,
  kernel = list(type = "exponential", power = 2),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{sumstats}{A data.frame of target summary statistics containing a
\code{corr} column.}

\item{XtX}{A p x p sparse LD matrix aligned to \code{sumstats}.}

\item{family}{A string: "gaussian", "binomial", or "poisson".}

\item{beta.external}{A \code{p x M} matrix of external coefficients, with no
intercept row.}

\item{multi.method}{External aggregation strategy: "ind", "PCA", or
"stacking".}

\item{optim.args}{A list of control arguments for the stacking optimizer.}

\item{criteria}{Selection criterion, as in \code{\link{BRIERs.selection}}.
Summary criteria are "Cp", "GIC" (need \code{TN}) and "pseu.val";
validation criteria require \code{X.val} and \code{y.val}.}

\item{X.val, y.val}{Held-out validation data, required for validation
criteria. \code{X.val} must already be standardized, and \code{y.val} must
also be standardized when \code{family = "gaussian"}, because BRIER.S
returns standardized coefficients and nothing is standardized internally.}

\item{XtX.val, sumstats.val}{Optional held-out LD matrix and summary
statistics used by \code{"pseu.val"}. Default to the fitting \code{XtX}
and \code{sumstats}.}

\item{TN}{Training cohort sample size, required for "Cp" and "GIC".}

\item{h2}{Optional heritability estimate for "Cp" and "GIC". Defaults to 0.}

\item{bounds}{A named list of length-2 numeric vectors giving the search box
for each eta. Defaults to \code{c(0, 10)} per external model.}

\item{init.grid}{Optional initial design, as in \code{\link{BRIERi.bopt}}.}

\item{init.points}{Number of random initial points when \code{init.grid} is
not supplied.}

\item{n.iter}{Number of sequential optimization steps after initialization.}

\item{acq}{Acquisition function: "ucb", "ei", or "poi".}

\item{kappa}{Exploration parameter for "ucb".}

\item{acq.eps}{Exploration parameter for "ei" and "poi".}

\item{kernel}{Gaussian-process kernel specification.}

\item{verbose}{Logical. Print optimizer progress.}

\item{...}{Further arguments passed to \code{\link{BRIERs.eta}}.}
}
\value{
An object of class \code{c("BRIER.bopt", "BRIER.selection", "BRIER")},
shaped exactly like a \code{\link{BRIERs.selection}} result with an added
\code{bopt} element recording the search.
}
\description{
Tunes the integration weight \code{eta} for \code{\link{BRIERs}} with
sequential model-based optimization instead of an exhaustive grid. Each
proposed \code{eta} triggers one summary-level fit over the full lambda
path, scored with the same criterion \code{\link{BRIERs.selection}} would
apply.
}
\examples{
\dontrun{
sel <- BRIERs.bopt(
  sumstats, ld$XtX,
  family        = "gaussian",
  beta.external = beta.external,
  criteria      = "gaussian.mspe",
  X.val         = standardize_X(X.val)[[1]],
  y.val         = standardize_X(as.matrix(y.val))[[1]],
  init.points   = 10,
  n.iter        = 15
)
}

}
\seealso{
\code{\link{BRIERs}}, \code{\link{BRIERs.selection}},
\code{\link{BRIERi.bopt}}, \code{\link{BRIERfull.bopt}}
}
