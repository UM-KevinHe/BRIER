% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/p2cor.R
\name{p2cor}
\alias{p2cor}
\title{Convert GWAS p-values to marginal correlations}
\usage{
p2cor(p, n, sign = rep(1, length(p)))
}
\arguments{
\item{p}{A numeric vector of p-values from marginal associations.}

\item{n}{A numeric scalar or vector of sample sizes. If scalar, the same
sample size is used for all p-values.}

\item{sign}{A numeric vector of the signs of the marginal coefficient
estimates (+1 or -1). Defaults to +1 for all.}
}
\value{
A numeric vector of marginal correlations (X'y / n).
}
\description{
Recover the marginal correlation between genotype and outcome from GWAS
summary statistics (p-values, sample sizes, and effect-size signs). Useful
for constructing the \code{corr} column of \code{sumstats} input to
\code{\link{BRIERs}}.
}
\examples{
p <- c(0.01, 0.015)
n <- c(100, 1000)
sign <- c(-1, 1)
p2cor(p, n, sign)

}
\seealso{
\code{\link{BRIERs}}
}
