% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BRIERs.R
\name{BRIERs}
\alias{BRIERs}
\title{BRIER.S: integration of pretrained external models with GWAS summary statistics}
\usage{
BRIERs(
  sumstats,
  XtX,
  family = c("gaussian", "binomial", "poisson"),
  eta.list = c(0, exp(seq(log(0.1), log(10), length.out = 20))),
  beta.external = rep(0, nrow(sumstats)),
  multi.method = c("ind", "PCA", "stacking", "PCstacking"),
  optim.args = list(),
  ...,
  trace = FALSE,
  ncores = max(1L, parallel::detectCores() - 1L),
  parallel = (ncores > 1L),
  pca.var = 0.8,
  dedup.cor = 0.9
)
}
\arguments{
\item{sumstats}{A data.frame of GWAS summary statistics with at minimum a
\code{corr} column (marginal correlation between genotype and outcome).
Optionally \code{CHR} and \code{BP} columns for variant naming, or a
\code{varnames} column for explicit names.}

\item{XtX}{A p x p sparse matrix representing the LD reference panel.
Will be coerced to \code{Matrix::sparseMatrix} if not already sparse.}

\item{family}{A string specifying the response distribution: "gaussian",
"binomial", or "poisson".}

\item{eta.list}{A list of numeric vectors (one per external model) specifying
the eta grid for that model. A single numeric vector is replicated for
all external models with a warning.}

\item{beta.external}{A numeric matrix of external model coefficients
(p x M, no intercept). Defaults to a vector of zeros.}

\item{multi.method}{A string specifying how to combine multiple external
models: "ind", "PCA", "stacking", or "PCstacking". See \code{\link{BRIERi}}.}

\item{optim.args}{A list of arguments passed to \code{optim()} when using
stacking.}

\item{...}{Additional arguments passed to \code{BRIERs.eta}.}

\item{trace}{Logical. If TRUE, prints progress messages during fitting.}

\item{ncores}{Integer. Number of cores for parallel fitting.}

\item{parallel}{Logical. If TRUE and on a non-Windows platform, fits the eta
grid in parallel using \code{parallel::mclapply}.}

\item{pca.var}{Fraction of the variance retained by the principal-component
reduction under \code{multi.method = "PCstacking"}. Ignored otherwise.}

\item{dedup.cor}{Similarity threshold for dropping redundant external models
before anything else runs, keeping the first occurrence of each
near-identical set. Particularly relevant here, since the stacking weights
are obtained by inverting \code{t(B) XtX B}, which is singular the moment
two external models carry the same information. \code{NULL} or \code{NA}
disables the step. See \code{\link{dedupExternals}}.}
}
\value{
An object of class \code{"BRIER"} containing:
\describe{
\item{XtY}{The marginal correlation vector.}
\item{XtY.external}{Computed external summary predictions.}
\item{beta.external}{The external coefficients (possibly aggregated).}
\item{family}{The response family.}
\item{eta.list}{The list of per-model eta grids.}
\item{eta.grid}{The full combinatorial eta grid.}
\item{res}{A list of \code{BRIER.eta} objects, one per eta combination.}
\item{null.dev}{Placeholder set to 0 (null deviance not defined for
summary-stat models).}
\item{varnames}{The variant names.}
\item{external.dedup}{The de-duplication record, or \code{NULL} when nothing
was removed.}
\item{external.pca}{The principal-component reduction record under
\code{"PCstacking"}, or \code{NULL} otherwise.}
}
}
\description{
Fits a penalized regression model that integrates one or more pretrained
external prediction models with target-cohort GWAS summary statistics and an
LD reference panel. External information is blended into the marginal
correlation vector via a tunable eta parameter (one per external model), and
lambda is selected over a regularisation path.
}
\examples{
\dontrun{
# sumstats: data.frame with 'corr', 'CHR', 'BP' columns
# XtX: sparse p x p LD matrix
# beta.external: p x M matrix of external coefficients

fit <- BRIERs(
  sumstats, XtX, family = "gaussian",
  eta.list = list(c(0, 0.5, 1, 2)),
  beta.external = beta.external,
  penalty = "LASSO"
)
}

}
\seealso{
\code{\link{BRIERs.eta}}, \code{\link{BRIERs.selection}},
\code{\link{BRIERi}}, \code{\link{BRIERfull}}
}
